name: Publish Package

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write

    steps:
     - name: Checkout code
       uses: actions/checkout@v6

     - name: "Set up Python"
       uses: astral-sh/setup-uv@v7
       with:
         enable-cache: true

     - name: "Build"
       run: uv build

     - name: "Publish"
       run: uv publish
